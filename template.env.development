REACT_APP_GRAPHQL_ENDPOINT=http://localhost:7250/graphql

# If you are testing the site from a device that is different
# than the one running the api replace "localhost" with the internal
# IP address of the machine running the api (usually something like 192.168.x.x)
#
# Example:
# REACT_APP_GRAPHQL_ENDPOINT=http://192.168.1.172:7250/graphql
